'use client';

/**
 * Company-namespaced re-export of the payment-instructions form. Same as the
 * contractor variant — keeps the company admin inside the company sidebar
 * chrome when they click "Payment Instructions" in the sidebar.
 */
export const dynamic = 'force-dynamic';
export { default } from '../../../settings/payment-methods/page';
